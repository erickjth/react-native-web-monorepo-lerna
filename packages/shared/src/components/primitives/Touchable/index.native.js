import NativeTouchable from 'react-native-platform-touchable';
import styled from 'styled-components';
import { viewStyle } from '../../../styles';

const Touchable = styled(NativeTouchable)`${viewStyle}`;

export default Touchable;
